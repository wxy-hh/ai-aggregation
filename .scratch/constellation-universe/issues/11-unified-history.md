# 11 — 统一历史记录集成

**What to build:** 已登录用户的确定性真值完成时，按现有统一历史记录规范写入一条「命理」类型结果（标题「星座寰宇 · {昵称}的本命星盘」、状态「解读整理中」），AI 解读完成后更新同一条记录；解读超时可从同一条记录只重试解读。用户从统一历史可重新打开该次结果；删除仅存在于全局历史记录，删除级联清除全部修订、出生档案、问答与分享资产并撤销分享令牌。匿名用户只保留当前会话临时记录，登录后经明确确认才迁移；未确认的临时记录会话结束后删除。

**Blocked by:** 06 — 结果页首屏

**Status:** ✅ 已完成（2026-09-01，Playwright A1–A6 + B1–B5 全绿 + 截图审毕；另修复暗色弹层 /92 透明度静默丢失 bug）

- [x] 真值完成即写入历史；结果页不提供删除、不另设收藏或报告中心（表单提交与仪式重试两处写入 `lib/astrology/history.ts`；登录→IndexedDB 统一历史，匿名→sessionStorage 临时记录，diag 断言两条路由）
- [x] 历史列表只显示昵称、模块、日期与低敏摘要（卡面=昵称+「星座寰宇」模块标签+精度·出生日期+主轴金句摘要+太阳×月亮徽章；时间未知且月亮跨座时徽章诚实退化为「本命星盘」——mock 未知档跨座星体 sign=null，属设计内行为）
- [x] 补全/提高时间精度重算更新同一逻辑记录并版本化；改日期或城市创建新记录（逻辑 id=出生日期+城市 djb2 哈希；A5 精度重算仍 1 条且旧版折叠进 revisions≤5 只读快照；A6 改日期 2 条）
- [x] 匿名临时记录迁移需明确确认，未确认会话结束即删（sessionStorage 标签页关闭即清；B 链路：匿名测算→入口首页临时记录卡「继续查看」→登录后底部抽屉迁移确认→「保存到历史记录」后历史页可见；暗色弹层已重截确认）
- [x] `pnpm typecheck` 与 `pnpm lint` 通过（另 `pnpm vitest run src/lib/astrology/` 93 测试绿）
