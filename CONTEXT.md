# Domain Glossary

## 八字合盘 / Bazi Compatibility

### 缘分卡
合盘结果的**脱敏图片分享物**（PNG），绑定用户当前选中的**关系视角**。  
不是完整合盘报告副本，也不承载双方生辰、地点、四柱全量或低分细节。

### 本视角适配分
随关系视角（恋爱 / 婚姻 / 朋友 / 合作）变化的分享卡主分数。  
由命盘底分、六维加权与关系事实偏置确定性合成；**不等于**四视角共用的命盘底分。

### 脱敏分享
默认仅暴露：双方展示称呼（「我」与对方展示名或「TA」）、关系类型、本视角适配分与分档、一句关系底色、可选一条行动建议，以及指向产品入口的二维码。  
精确出生日期、时间、地点、四柱全量、六维明细与具体低分维度不进入分享物。

### 关系视角
合盘报告的四个解读切片之一：恋爱、婚姻、朋友、合作。  
缘分卡导出时使用**当前激活视角**，所见即所得。

### 六维维度说明
关系视角下六个维度各自的分数解释文案（维度 `note`）。  
有说明时在图谱/矩阵卡片上展示；没有则不硬塞占位。

### 视角补生成
对同一合盘档案，首次未覆盖的关系视角按需生成解读。  
与首开合盘一样消耗额度；已缓存视角的纯切换不请求、不扣费。

## 星座寰宇 / Astrology

### 星盘轮
星座模块渲染星盘事实的唯一深模块（`<AstrologyWheel />`）。  
内部收敛 WebGL 场景探测、按需分包、三级降级、3D 浑天仪舞台包装与激活态（`isActive`）全局 store 订阅。

### 星渊场景
基于 WebGL/Three.js 的深空星盘 3D 交互场景（`AstrologyWheelScene`）。  
通过首帧就绪状态机（`scenePainted`）实现自 SVG 兜底平滑交接，模块切走时通过帧循环停摆（`FrameloopGovernor`）停止 GPU 空转。

### SVG 星盘轮
基于矢量图形的标准星盘展示组件（`AstrologyChartWheel`）。  
用于仪式阶段揭秘动画、移动端表单缩略图、WebGL 不可用时的无缝兜底以及入口首页示例盘（`scene={false}` 模式）。

