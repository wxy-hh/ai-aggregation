worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;

    # 请求体大小限制（文件上传）
    client_max_body_size 50m;

    # ---------- HTTP → HTTPS 强制跳转 ----------
    # 重要：生产环境 Cookie 带 Secure 属性，浏览器拒收 HTTP 明文页面下发的
    # Secure Cookie，会导致登录态丢失并触发匿名登录兜底。
    # 因此启用 SSL 后必须拆成两个 server 块：80 只做跳转（和 ACME 续签），
    # 业务流量只走 443，禁止在同一个 server 块里同时监听 80 和 443。
    server {
        listen 80 default_server;
        server_name your-domain.com;

        # ACME challenge（证书续签用，必须走 HTTP 80）
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # 其余全部跳转 HTTPS
        location / {
            return 301 https://$host$request_uri;
        }
    }

    # ---------- 主站（仅 HTTPS）----------
    server {
        listen 443 ssl;
        http2 on;
        server_name your-domain.com;

        # ---------- SSL 证书（Let's Encrypt）----------
        ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # ACME challenge（证书续签用）
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # Next.js 静态资源（长缓存）
        location /_next/static/ {
            proxy_pass http://web:3000;
            proxy_cache_valid 200 365d;
            add_header Cache-Control "public, max-age=31536000, immutable";
        }

        # 公共静态资源
        location /public/ {
            proxy_pass http://web:3000;
        }

        # SSE 流式接口（对话等）—— 关闭缓冲
        location /api/chat {
            proxy_pass http://web:3000;
            proxy_http_version 1.1;
            proxy_set_header Connection '';
            proxy_buffering off;
            proxy_cache off;
            chunked_transfer_encoding off;
            proxy_read_timeout 300s;
        }

        # 命理分析等长耗时接口
        location /api/destiny/ {
            proxy_pass http://web:3000;
            proxy_http_version 1.1;
            proxy_set_header Connection '';
            proxy_buffering off;
            proxy_read_timeout 300s;
        }

        # WebSocket（实时语音等）
        location /api/voice/realtime {
            proxy_pass http://web:3000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 3600s;
        }

        # 默认：所有请求代理到 Web
        location / {
            proxy_pass http://web:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
